#include <stdio.h>
/**
 * main - print alphabet
 *
 * Description: print lower and uppercase alphabet
 * Return: 0
 */
int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);
	for (ch = 'a' ch <= 'z'; ch++)
		putchar(ch);
	putchar('\n')
	return (0);
}
