#ifndef ABS_H

#define ABS_H
/**
 * file: 3-function_like_macro.h
 * Description: a function-like macro ABS(x) that computes
 * the absolute value of a number x
 * @x: number we are finding absolute of
 */
#define ABS(x) ((x) < 0 ? (-x) : (x))

#endif
