#include "main.h"
/**
 * _islower - lowercase
 *
 * Description: check if alphabet is a lower case
 * @c: the integer value it receives
 * Return: 0
 */
int _islower(int c)
{
	int ch;

	(islower(ch) ? 1 : 0;
	return (0);
}
